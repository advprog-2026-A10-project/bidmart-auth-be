// Intentionally left empty.
//
// The previous placeholder mocks in this folder depended on the old
// `modules::example` scaffold and obsolete trait names. They were removed
// after the module rename to `modules::auth` to avoid stale references.
